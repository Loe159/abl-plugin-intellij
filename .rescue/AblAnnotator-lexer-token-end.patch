diff --git a/src/main/kotlin/com/ablls/plugin/annotator/AblAnnotator.kt b/src/main/kotlin/com/ablls/plugin/annotator/AblAnnotator.kt
index ee0f895..25841db 100644
--- a/src/main/kotlin/com/ablls/plugin/annotator/AblAnnotator.kt
+++ b/src/main/kotlin/com/ablls/plugin/annotator/AblAnnotator.kt
@@ -3,6 +3,7 @@ package com.ablls.plugin.annotator
 import com.ablls.plugin.core.AblParseResult
 import com.ablls.plugin.core.AblProjectAnalysisService
 import com.ablls.plugin.core.SyntaxError
+import com.ablls.plugin.parser.AblLexerAdapter
 import com.ablls.plugin.language.AblLanguage
 import com.intellij.lang.annotation.AnnotationHolder
 import com.intellij.lang.annotation.ExternalAnnotator
@@ -55,7 +56,10 @@ class AblAnnotator : ExternalAnnotator<AblAnnotator.Input, List<SyntaxError>>()
             val lineStart  = document.getLineStartOffset(line)
             val lineEnd    = document.getLineEndOffset(line)
             val colStart   = (lineStart + error.column).coerceAtMost(lineEnd)
-            val colEnd     = if (colStart < lineEnd) (colStart + 1) else colStart
+            val lexer      = AblLexerAdapter()
+            lexer.start(document.charsSequence, colStart, lineEnd)
+            val tokenEnd   = lexer.tokenEnd
+            val colEnd     = tokenEnd.coerceAtLeast(colStart + 1)
             val range      = TextRange(colStart, colEnd.coerceAtMost(docLength))
 
             holder.newAnnotation(HighlightSeverity.ERROR, error.message)
